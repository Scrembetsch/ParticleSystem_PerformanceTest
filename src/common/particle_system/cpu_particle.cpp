#include "cpu_i_module.h"

//void Particles::resize(size_t newSize)
//{
//    Positions.resize(newSize);
//    Velocities.resize(newSize);
//    Colors.resize(newSize);
//    Lifetimes.resize(newSize);
//    Active.resize(newSize);
//    Seeds.resize(newSize);
//}